import { Feed } from '../interface/interface';
import Firebase from './../database/index';
import {MessageSuccess, MessageError} from './../shares/message'

const database = Firebase.admin.database();

const CreateFeed = (formData:any) => {

}